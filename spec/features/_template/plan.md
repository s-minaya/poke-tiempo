# NNN · <Nombre de la feature> — Plan

_Al copiar esta plantilla, renombra este archivo a `NNN-plan.md` (con el número real de la feature). Ver `AGENTS.md`._

_Cómo se implementa lo descrito en `NNN-spec.md`. Debe respetar la `constitution/` (mission.md y tech-stack.md)._

## Enfoque

<Estrategia general en pocas frases: qué aproximación se toma y por qué encaja con el stack (React + TypeScript, Sass + BEM, mobile-first, sin backend) y con la identidad del proyecto (pixel art / Pokédex).>

## Implementación

_Pasos técnicos concretos, en orden. Indica los archivos/módulos que se tocan (componentes en `src/components/`, lógica pura en `src/domain/`, estilos `.scss` con BEM, breakpoints desde `_breakpoints.scss`, datos en `src/data/`, scripts de build en `scripts/`)._

1. <Paso — archivo/módulo afectado.>
2. <Paso — archivo/módulo afectado.>
3. <Paso — archivo/módulo afectado.>

## Decisiones

_Elecciones de diseño relevantes y su justificación. Alternativas descartadas y por qué._

- **<Decisión>** — <por qué; qué se descartó>.
- **<Decisión>** — <por qué; qué se descartó>.

## Riesgos

_Qué puede salir mal o requerir cuidado, y cómo se mitiga (rendimiento, accesibilidad, coherencia visual, dependencia de AEMET, licencias, etc.)._

- **<Riesgo>** — <mitigación>.
